package modele.utilitaires;

public class Enum {
    
    // A changer avec les bonnes valeurs
    public enum sens {
        NORD,
        SUD,
        EST,
        OUEST
    }

    // A changer avec les bonnes valeurs
    public enum presenceAnomalie {
        Forte,
        Faible
    }

    // A changer avec les bonnes valeurs
    public enum jour {
        LUNDI,
        MARDI,
        MERCREDI,
        JEUDI,
        VENDREDI,
        SAMEDI,
        DIMANCHE
    }

    // A changer avec les bonnes valeurs
    public enum vacances {
        OUI,
        NON
    }
}
